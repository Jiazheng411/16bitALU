// remainder computes a % b

module remainder (
    input a[16],  // divisor
    input b[16],  // dividend
    input quotient[16], //quotient from dividor
    output out[16]
  ) {
  always {
    out = a - b * quotient;
  }
}
